# Simple_ImageList_React_Native
### This is a simple app to display list of image using react native

------------


Install all dependecies : <br/>
`npm install` or `yarn install` <br/>
<br/>
Run The App : <br/>
`expo start`
